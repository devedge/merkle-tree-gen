(function () {

    'use strict';

    var hasha = require('hasha');
    var FastMap = require('collections/fast-map');

    function HashArray(hA = 'sha256', pL = false) {
        
    }

    HashArray.prototype.hashElements = function hashElements() {
        
    }

    module.exports = HashArray;

})();