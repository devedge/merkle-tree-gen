// import merkle-tree-gen manually
var merkle = require('../index');
var assert = require('assert');


// first, hash a < 1 MiB file. 

// then, hash a several MiB file

// then, try to hash a file with a size of 0 MiB

// hash a file that is evenly 3 MiB

// hash a file just under 3 MiB

// hash a file just over 3 MiB
